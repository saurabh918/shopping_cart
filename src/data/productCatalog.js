/**
 * Default storefront catalog (6 products). Staging is wired via CRACO alias when
 * REACT_APP_USE_STAGING_CATALOG=true (see craco.config.js and npm run start:staging).
 */
export * from "./productCatalog.production";
